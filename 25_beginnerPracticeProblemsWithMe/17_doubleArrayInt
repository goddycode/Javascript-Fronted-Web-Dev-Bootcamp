/*
This script takes array of integers, and return a new array with each value doubled
*/

function maps(x){
	const newArr = [];
	for(let i = 0; i < x.length; i++){
		newArr = x[i] * 2;
	}
	return newArr;
}
