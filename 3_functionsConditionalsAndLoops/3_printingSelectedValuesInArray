/*
Script which select some datatypes
*/

const myArray = ['lucy', 'mercy', 'mango', 'muler', 20, 20, 40, 50];

for (let i = 0; i <= myArray.length; i++) {
	if (typeof myArray[i] === 'number') {
		console.log(myArray[i]);
	}
}
